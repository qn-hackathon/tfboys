# 安装与启动指南

本文档提供**文创智推：文字创作在短视频推广的加速平台**的环境初始化、安装和启动运行完整指南。

> **注**: TFBoys 是团队名称，项目是文创智推平台。

## 📋 目录

- [系统要求](#系统要求)
- [环境准备](#环境准备)
- [安装步骤](#安装步骤)
- [启动方式](#启动方式)
- [验证测试](#验证测试)
- [常见问题](#常见问题)

---

## 系统要求

### 硬件要求
- **CPU**: 4 核心以上
- **内存**: 8GB 以上（推荐 16GB）
- **磁盘**: 至少 20GB 可用空间

### 软件要求

#### 方式一：Docker 部署（推荐）
- **Docker**: 20.10.0 或更高版本
- **Docker Compose**: 2.0.0 或更高版本
- **操作系统**: Linux（推荐 Ubuntu 20.04+）/ macOS / Windows with WSL2

#### 方式二：本地开发
- **Python**: 3.10 或更高版本
- **Node.js**: 16.0 或更高版本
- **Redis**: 5.0 或更高版本
- **操作系统**: macOS / Linux / Windows with WSL2

---

## 环境准备

### 方式一：Docker 环境

#### 1. 安装 Docker

**macOS**:
```bash
# 使用 Homebrew 安装
brew install --cask docker

# 或下载 Docker Desktop
# https://www.docker.com/products/docker-desktop
```

**Ubuntu/Debian**:
```bash
# 更新软件包索引
sudo apt-get update

# 安装依赖
sudo apt-get install ca-certificates curl gnupg lsb-release

# 添加 Docker 官方 GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 设置稳定版仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker Engine
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

#### 2. 验证 Docker 安装

```bash
# 检查 Docker 版本
docker --version
# 预期输出: Docker version 20.10.x 或更高

# 检查 Docker Compose 版本
docker compose version
# 预期输出: Docker Compose version v2.x.x 或更高

# 测试 Docker 运行
docker run hello-world
```

### 方式二：本地开发环境

#### 1. 安装 Python 3.10+

**macOS**:
```bash
# 使用 Homebrew 安装
brew install python@3.10
```

**Ubuntu/Debian**:
```bash
sudo apt-get update
sudo apt-get install python3.10 python3.10-venv python3-pip
```

验证安装:
```bash
python3 --version  # 应显示 Python 3.10.x 或更高
```

#### 2. 安装 Node.js 16+

**macOS**:
```bash
brew install node@16
```

**Ubuntu/Debian**:
```bash
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs
```

验证安装:
```bash
node --version  # 应显示 v16.x.x 或更高
npm --version
```

#### 3. 安装 Redis

**macOS**:
```bash
brew install redis
brew services start redis
```

**Ubuntu/Debian**:
```bash
sudo apt-get install redis-server
sudo systemctl start redis-server
sudo systemctl enable redis-server
```

验证安装:
```bash
redis-cli ping  # 应返回 PONG
```

---

## 安装步骤

### 方式一：Docker 部署（推荐）

#### 1. 克隆代码仓库

```bash
git clone https://github.com/qn-hackathon/tfboys.git
cd tfboys
```

#### 2. 配置环境变量

```bash
# 复制 AI Service 环境变量模板
cp backend/ai-service/.env.example backend/ai-service/.env

# 编辑配置文件
vim backend/ai-service/.env
# 或使用其他编辑器: nano, code 等
```

**必填配置项**:
```env
# 七牛 AI Token API Key（必填）
QINIU_API_KEY=sk-your-actual-api-key-here

# 以下配置在 Docker 环境中使用默认值即可
REDIS_URL=redis://redis:6379/0
VIDEO_SERVICE_URL=http://video-service:8003
LOCAL_STORAGE_DIR=/tmp/tfboys
```

#### 3. 构建并启动服务

```bash
# 构建 Docker 镜像并启动所有服务
docker compose up --build -d

# 查看服务状态
docker compose ps
```

预期输出所有服务状态为 `Up` 或 `running`：
```
NAME                    STATUS              PORTS
tfboys-redis            Up (healthy)        0.0.0.0:6379->6379/tcp
tfboys-api-gateway      Up                  0.0.0.0:8001->8001/tcp
tfboys-ai-service       Up (healthy)        0.0.0.0:8002->8002/tcp
tfboys-ai-worker        Up                  
tfboys-video-service    Up (healthy)        0.0.0.0:8003->8003/tcp
tfboys-video-worker     Up                  
tfboys-frontend         Up                  0.0.0.0:3000->3000/tcp
```

### 方式二：本地开发

#### 1. 克隆代码仓库

```bash
git clone https://github.com/qn-hackathon/tfboys.git
cd tfboys
```

#### 2. 安装 Shared 模块

```bash
cd shared
pip install -e .
cd ..
```

#### 3. 配置环境变量

```bash
# 复制 AI Service 环境变量模板
cp backend/ai-service/.env.example backend/ai-service/.env

# 编辑配置文件，填入真实的 API Key
vim backend/ai-service/.env
```

**配置示例**:
```env
# 七牛 AI Token API Key（必填）
QINIU_API_KEY=sk-your-actual-api-key-here

# Redis 配置
REDIS_URL=redis://localhost:6379/0

# Video Service 配置
VIDEO_SERVICE_URL=http://localhost:8003

# 本地存储目录
LOCAL_STORAGE_DIR=/tmp/tfboys
```

#### 4. 安装后端依赖

```bash
# 设置 PYTHONPATH
export PYTHONPATH="$(pwd)"

# 安装 API Gateway 依赖
cd backend/api-gateway
pip install -r requirements.txt
cd ../..

# 安装 AI Service 依赖
cd backend/ai-service
pip install -r requirements.txt
cd ../..

# 安装 Video Service 依赖
cd backend/video-service
pip install -r requirements.txt
cd ../..
```

#### 5. 安装前端依赖

```bash
cd frontend
npm install
cd ..
```

---

## 启动方式

### 方式一：Docker 启动

#### 一键启动所有服务

```bash
# 后台启动所有服务
docker compose up -d

# 查看服务日志
docker compose logs -f

# 按 Ctrl+C 退出日志查看（不会停止服务）
```

#### 单独启动特定服务

```bash
# 启动 Redis
docker compose up -d redis

# 启动 AI Service
docker compose up -d ai-service ai-worker

# 启动 Video Service
docker compose up -d video-service video-worker

# 启动 API Gateway
docker compose up -d api-gateway

# 启动前端
docker compose up -d frontend
```

### 方式二：本地开发启动

本地开发需要 **5 个终端窗口** 来分别启动各个服务。

#### 终端 1: 启动 API Gateway

```bash
cd backend/api-gateway
export PYTHONPATH="$(cd ../.. && pwd)"
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

#### 终端 2: 启动 AI Service

```bash
cd backend/ai-service
export PYTHONPATH="$(cd ../.. && pwd)"
uvicorn app.main:app --reload --host 0.0.0.0 --port 8002
```

#### 终端 3: 启动 AI Service Celery Worker

```bash
cd backend/ai-service
export PYTHONPATH="$(cd ../.. && pwd)"
celery -A app.workers.celery_app worker --loglevel=info
```

#### 终端 4: 启动 Video Service

```bash
cd backend/video-service
export PYTHONPATH="$(cd ../.. && pwd)"
uvicorn app.main:app --reload --host 0.0.0.0 --port 8003
```

#### 终端 5: 启动前端

```bash
cd frontend
npm run dev
```

---

## 验证测试

### 1. 健康检查

```bash
# 检查 API Gateway
curl http://localhost:8001/health
# 预期输出: {"status":"healthy"}

# 检查 AI Service
curl http://localhost:8002/health
# 预期输出: {"status":"healthy"}

# 检查 Video Service
curl http://localhost:8003/health
# 预期输出: {"status":"healthy"}

# 检查 Redis
redis-cli ping
# 预期输出: PONG

# （Docker 环境）
docker exec tfboys-redis redis-cli ping
```

### 2. 访问前端

在浏览器中打开：
```
http://localhost:3000
```

应该能看到 TFBoys 的 Web 界面。

### 3. API 端到端测试

#### 方式 1：使用内置测试脚本（推荐）

**Docker 环境**:
```bash
docker exec -it tfboys-ai-service ./test_e2e.sh
```

**本地环境**:
```bash
cd backend/ai-service
./test_e2e.sh
```

#### 方式 2：手动 API 测试

```bash
# 创建任务
curl -X POST http://localhost:8001/api/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "novel_text": "小明是一个活泼开朗的男孩。今天早上，小明起床后刷牙洗脸，然后去上学。"
  }'

# 记录返回的 task_id，例如：task_12345

# 查询任务状态
curl http://localhost:8001/api/tasks/task_12345

# 查看所有任务
curl http://localhost:8001/api/tasks
```

**预期输出（任务创建成功）**:
```json
{
  "task_id": "task_12345",
  "status": "queued",
  "created_at": "2025-10-26T10:00:00Z"
}
```

**预期输出（任务完成）**:
```json
{
  "task_id": "task_12345",
  "status": "completed",
  "video_url": "http://localhost:8003/videos/task_12345/final.mp4",
  "progress": {
    "text_analysis": "completed",
    "image_generation": "completed",
    "voice_generation": "completed",
    "video_composition": "completed"
  }
}
```

---

## 常见问题

### 1. Docker 容器无法启动

**问题**: `docker compose up` 后某些容器状态为 `Exited`

**解决方案**:
```bash
# 查看容器日志
docker compose logs ai-service
docker compose logs ai-worker

# 检查端口是否被占用
lsof -i :8001
lsof -i :8002
lsof -i :8003
lsof -i :6379

# 重新构建并启动
docker compose down
docker compose up --build -d
```

### 2. Redis 连接失败

**问题**: 服务日志显示 `ConnectionRefusedError`

**解决方案**:

**Docker 环境**:
```bash
# 检查 Redis 容器状态
docker compose ps redis

# 重启 Redis
docker compose restart redis

# 测试连接
docker exec tfboys-redis redis-cli ping
```

**本地环境**:
```bash
# 检查 Redis 服务状态
brew services list  # macOS
sudo systemctl status redis-server  # Linux

# 启动 Redis
brew services start redis  # macOS
sudo systemctl start redis-server  # Linux
```

### 3. Celery Worker 未消费任务

**问题**: 任务状态一直是 `queued`，未被处理

**解决方案**:

**Docker 环境**:
```bash
# 查看 Worker 日志
docker compose logs -f ai-worker

# 重启 Worker
docker compose restart ai-worker

# 检查任务队列
docker exec tfboys-redis redis-cli LLEN celery
```

**本地环境**:
```bash
# 检查 Worker 进程
ps aux | grep celery

# 重启 Worker
# 在 Worker 终端按 Ctrl+C 停止，然后重新启动：
cd backend/ai-service
export PYTHONPATH="$(cd ../.. && pwd)"
celery -A app.workers.celery_app worker --loglevel=info
```

### 4. 前端无法访问

**问题**: 浏览器访问 `http://localhost:3000` 失败

**解决方案**:

**Docker 环境**:
```bash
# 检查前端容器状态
docker compose ps frontend

# 查看前端日志
docker compose logs -f frontend

# 重启前端
docker compose restart frontend
```

**本地环境**:
```bash
# 检查端口占用
lsof -i :3000

# 清理依赖并重新安装
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 5. API 返回 500 错误

**问题**: 测试 API 时返回 `Internal Server Error`

**解决方案**:
```bash
# 查看服务日志
docker compose logs -f ai-service  # Docker
# 或查看终端输出（本地开发）

# 检查环境变量配置
docker exec tfboys-ai-service env | grep QINIU_API_KEY  # Docker
# 或本地环境：
cd backend/ai-service
cat .env | grep QINIU_API_KEY
```

### 6. 权限错误 (Permission Denied)

**问题**: 日志显示 `/tmp/tfboys` 权限不足

**解决方案**:

**Docker 环境**:
```bash
# 进入容器修复权限
docker exec -it tfboys-ai-service chmod -R 777 /tmp/tfboys
```

**本地环境**:
```bash
# 创建并设置权限
sudo mkdir -p /tmp/tfboys
sudo chmod -R 777 /tmp/tfboys
```

---

## 服务端口总览

| 服务 | 端口 | 用途 | 健康检查 |
|------|------|------|----------|
| 前端 | 3000 | Web UI | http://localhost:3000 |
| API Gateway | 8001 | 用户请求入口 | http://localhost:8001/health |
| AI Service | 8002 | AI 处理服务 | http://localhost:8002/health |
| Video Service | 8003 | 视频合成服务 | http://localhost:8003/health |
| Redis | 6379 | 数据存储和消息队列 | `redis-cli ping` |

---

## 停止服务

### Docker 环境

```bash
# 停止所有服务（保留数据）
docker compose stop

# 停止并删除容器（保留数据卷）
docker compose down

# 停止并删除所有数据（⚠️ 会清空数据）
docker compose down -v
```

### 本地开发

在各个终端窗口按 `Ctrl+C` 停止对应服务。

---

## 下一步

- 查看 [架构设计文档.md](./架构设计文档.md) 了解系统架构
- 查看 [团队分工文档.md](./团队分工文档.md) 了解模块职责
- 查看 [CLAUDE.md](./CLAUDE.md) 了解开发规范和项目结构

---

**最后更新**: 2025-10-26  
**维护者**: TFBoys Team
