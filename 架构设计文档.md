# TFBoys - 架构设计文档

本文档提供 TFBoys 文字生成视频系统的完整架构设计说明。

## 📋 目录

- [系统概述](#系统概述)
- [技术栈](#技术栈)
- [系统架构](#系统架构)
- [服务架构](#服务架构)
- [数据模型](#数据模型)
- [核心业务流程](#核心业务流程)
- [存储架构](#存储架构)
- [技术难点与解决方案](#技术难点与解决方案)
- [扩展性设计](#扩展性设计)

---

## 系统概述

### 项目简介

TFBoys (Token Free Boys) 是一个自动化的文字生成视频系统，能够将小说文字自动转换为动漫风格的视频。视频由图片、文字和配音三部分组成，并保证角色的视觉一致性。

### 核心特性

- 🎨 **角色一致性**: 通过详细的提示词描述保证角色在不同场景的视觉一致性
- 🤖 **AI 驱动**: 基于七牛 AI Token API，使用 DeepSeek-V3 进行文本分析，Gemini 2.5 Flash 生成图像，七牛 TTS 生成配音
- 🎬 **自动化视频合成**: 使用 FFmpeg 自动合成图片、字幕和音频
- 📦 **Monorepo 架构**: 前后端代码在同一仓库，便于管理和部署
- 🐳 **Docker 支持**: 完整的 Docker 配置，一键启动所有服务

---

## 技术栈

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18+ | UI 框架 |
| TypeScript | 4.9+ | 类型安全 |
| Vite | 4+ | 构建工具 |
| Ant Design | 5+ | UI 组件库 |
| Axios | 1+ | HTTP 客户端 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10+ | 后端语言 |
| FastAPI | 0.104+ | Web 框架 |
| Celery | 5.3+ | 异步任务队列 |
| Redis | 5.0+ | 缓存和消息队列 |
| Pydantic | 2.0+ | 数据验证 |
| httpx | 0.25+ | 异步 HTTP 客户端 |

### AI 服务

| 服务 | 用途 |
|------|------|
| DeepSeek-V3 (七牛 AI Token API) | 文本分析、场景分割 |
| Gemini 2.5 Flash (七牛文生图 API) | 图像生成 |
| 七牛 TTS | 配音生成 |

### 视频处理

| 工具 | 用途 |
|------|------|
| FFmpeg | 视频合成、字幕叠加、音频同步 |

### 存储

| 技术 | 用途 |
|------|------|
| 本地文件系统 | 图片、音频、视频文件存储 (`/tmp/tfboys`) |
| Redis | 任务状态管理、消息队列 |

---

## 系统架构

### 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                          用户层                                   │
│                  Web Browser (React UI)                          │
└────────────────────────┬─────────────────────────────────────────┘
                         │ HTTP/REST
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                       API Gateway (8001)                          │
│                    用户请求入口、路由转发                          │
└───────────────┬────────────────────────┬─────────────────────────┘
                │                        │
                ▼                        ▼
┌────────────────────────┐      ┌────────────────────────┐
│   AI Service (8002)    │◄────►│ Video Service (8003)   │
│   - 文本分析           │      │   - 视频合成            │
│   - 图像生成           │      │   - 字幕叠加            │
│   - 配音生成           │      │   - 音频同步            │
└───────┬────────────────┘      └───────┬────────────────┘
        │                               │
        ▼                               ▼
┌────────────────────────┐      ┌────────────────────────┐
│  Celery Worker (AI)    │      │ Celery Worker (Video)  │
│  异步处理 AI 任务      │      │ 异步处理视频合成任务    │
└───────┬────────────────┘      └───────┬────────────────┘
        │                               │
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │         Redis (6379)          │
        │   - 任务状态管理              │
        │   - Celery 消息队列           │
        │   - Celery 结果后端           │
        └───────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  本地文件存储 (/tmp/tfboys)   │
        │   - scenes/: 场景图像         │
        │   - audio/: 配音文件          │
        │   - videos/: 最终视频         │
        └───────────────────────────────┘
```

### 网络架构（Docker 环境）

```
┌─────────────────────────────────────────────────────────────┐
│                      Docker Network                          │
│                    (tfboys-network)                          │
│                                                               │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐ │
│  │  Redis   │   │   API    │   │    AI    │   │  Video   │ │
│  │  :6379   │◄──│ Gateway  │◄──│ Service  │◄──│ Service  │ │
│  │          │   │  :8001   │   │  :8002   │   │  :8003   │ │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘ │
│       ▲              │               │               │       │
│       │              │               │               │       │
│       │         ┌────┴────┐     ┌────┴────┐     ┌────┴────┐ │
│       │         │ Celery  │     │ Celery  │     │ Celery  │ │
│       └─────────┤ Worker  │     │ Worker  │     │ Worker  │ │
│                 │   (AI)  │     │   (AI)  │     │ (Video) │ │
│                 └─────────┘     └─────────┘     └─────────┘ │
│                                                               │
│  共享存储卷 (tfboys-storage): /tmp/tfboys                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 服务架构

### 1. API Gateway (端口 8001)

**职责**:
- 用户请求入口
- 请求路由和转发
- CORS 配置
- 请求日志记录

**主要 API 端点**:

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/tasks` | 创建视频生成任务 |
| GET | `/api/tasks/{task_id}` | 获取任务详情 |
| GET | `/api/tasks` | 获取任务列表 |
| DELETE | `/api/tasks/{task_id}` | 删除任务 |
| GET | `/health` | 健康检查 |

### 2. AI Service (端口 8002)

**职责**:
- 文本分析与场景分割（DeepSeek-V3）
- 图像生成（Gemini 2.5 Flash）
- 配音生成（七牛 TTS）
- 场景数据管理
- 任务状态更新

**主要内部端点**:

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/internal/tasks` | 创建 AI 处理任务 |
| GET | `/internal/tasks/{task_id}` | 获取任务状态 |
| POST | `/callbacks/video-completed` | 视频完成回调 |
| GET | `/health` | 健康检查 |

**核心组件**:

```python
# 文本分析器
class TextAnalyzer:
    """使用 DeepSeek-V3 分析小说文本，提取场景和角色信息"""
    async def analyze_novel(novel_text: str) -> List[Scene]

# 图像生成器
class ImageGenerator:
    """使用 Gemini 2.5 Flash 生成场景图像"""
    async def generate_scene_image(scene_desc: str) -> str

# 配音生成器
class VoiceGenerator:
    """使用七牛 TTS 生成配音"""
    async def generate_voice(text: str, voice: str) -> str

# Video Service 客户端
class VideoClient:
    """调用 Video Service 进行视频合成"""
    async def submit_synthesis_job(task_id: str, scenes: List[Scene])
```

### 3. Video Service (端口 8003)

**职责**:
- 接收场景数据
- 使用 FFmpeg 合成视频
- 字幕叠加
- 音频同步
- 场景拼接
- 视频文件存储

**主要内部端点**:

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/internal/video-synthesis/jobs` | 提交视频合成任务 |
| GET | `/internal/status/{task_id}` | 获取合成状态 |
| GET | `/health` | 健康检查 |

**FFmpeg 处理流程**:

```bash
# 1. 单场景合成（图 + 音 + 字幕）
ffmpeg -loop 1 -i scene.png \
  -i audio.mp3 \
  -vf "drawtext=text='旁白文字':fontfile=/usr/share/fonts/...:fontsize=24:..." \
  -shortest \
  -c:v libx264 -c:a aac \
  scene.mp4

# 2. 多场景拼接
ffmpeg -f concat -safe 0 -i scenes.txt \
  -c copy \
  final.mp4
```

### 4. Celery Workers

**AI Service Worker**:
- 异步处理小说文本
- 调用 AI 服务生成场景数据
- 管理任务状态
- 提交视频合成任务

**Video Service Worker**:
- 异步处理视频合成
- 调用 FFmpeg 生成视频
- 回调 AI Service 更新状态

**核心任务**:

```python
# AI Service Worker 任务
@celery_app.task(name="process_novel_task")
def process_novel_task(task_id: str, novel_text: str):
    """
    处理小说生成视频的完整流程
    1. 文本分析
    2. 图像生成
    3. 配音生成
    4. 提交视频合成任务
    """
    pass

# Video Service Worker 任务
@celery_app.task(name="compose_video_task")
def compose_video_task(task_id: str, scenes: List[dict]):
    """
    视频合成任务
    1. 下载场景图片和音频
    2. FFmpeg 合成单个场景
    3. FFmpeg 拼接所有场景
    4. 回调 AI Service
    """
    pass
```

---

## 数据模型

### Task（任务）

```python
class Task(BaseModel):
    """视频生成任务"""
    task_id: str              # 任务 ID
    status: TaskStatus        # pending | queued | processing | completed | failed
    novel_text: str           # 小说文本
    created_at: datetime      # 创建时间
    updated_at: datetime      # 更新时间
    progress: TaskProgress    # 进度信息
    error_message: Optional[str] = None  # 错误信息
    video_url: Optional[str] = None      # 视频 URL
```

### TaskProgress（任务进度）

```python
class TaskProgress(BaseModel):
    """任务进度"""
    text_analysis: str        # pending | processing | completed | failed
    image_generation: str     # pending | processing | completed | failed
    voice_generation: str     # pending | processing | completed | failed
    video_composition: str    # pending | processing | completed | failed
    total_scenes: int = 0     # 总场景数
    processed_scenes: int = 0 # 已处理场景数
```

### Scene（场景）

```python
class Scene(BaseModel):
    """场景数据"""
    scene_id: str             # 场景 ID
    scene_index: int          # 场景序号
    description: str          # 场景描述
    narration: str            # 旁白文字
    characters: List[str]     # 角色列表
    image_path: Optional[str] = None   # 图片路径
    audio_path: Optional[str] = None   # 音频路径
    duration: float = 0.0     # 持续时长（秒）
```

### Character（角色）

```python
class Character(BaseModel):
    """角色信息"""
    character_id: str         # 角色 ID
    name: str                 # 角色名称
    description: str          # 角色描述（用于图像生成）
    reference_image: Optional[str] = None  # 参考图片路径
```

---

## 核心业务流程

### 完整数据流程

```
1. 用户提交小说文本
   ↓
   [前端] POST /api/tasks → [API Gateway]
   ↓
2. API Gateway 转发请求
   ↓
   [API Gateway] POST /internal/tasks → [AI Service]
   ↓
3. AI Service 创建任务并入队
   ↓
   [AI Service] 将任务写入 Redis
   [AI Service] 发送 Celery 任务到队列
   ↓
4. Celery Worker 处理任务
   ↓
   [AI Worker] 步骤 4.1: 文本分析
   - 调用 DeepSeek-V3 分析小说
   - 提取场景列表和角色信息
   - 更新任务进度: text_analysis = "completed"
   ↓
   [AI Worker] 步骤 4.2: 图像生成
   - 为每个场景调用 Gemini 2.5 Flash
   - 生成场景图像
   - 保存到 /tmp/tfboys/scenes/
   - 更新任务进度: image_generation = "processing" → "completed"
   ↓
   [AI Worker] 步骤 4.3: 配音生成
   - 为每个场景调用七牛 TTS
   - 生成配音文件
   - 保存到 /tmp/tfboys/audio/{task_id}/
   - 更新任务进度: voice_generation = "processing" → "completed"
   ↓
   [AI Worker] 步骤 4.4: 提交视频合成任务
   - 调用 Video Service API
   - 传递场景数据包
   ↓
5. Video Service 处理视频合成
   ↓
   [Video Worker] 步骤 5.1: 单场景合成
   - 使用 FFmpeg 合成每个场景（图 + 音 + 字幕）
   - 保存到 /tmp/tfboys/videos/{task_id}/scene_*.mp4
   ↓
   [Video Worker] 步骤 5.2: 多场景拼接
   - 使用 FFmpeg concat 拼接所有场景
   - 生成最终视频 /tmp/tfboys/videos/{task_id}/final.mp4
   ↓
   [Video Worker] 步骤 5.3: 回调 AI Service
   - POST /callbacks/video-completed
   - 更新任务状态: status = "completed"
   - 更新任务进度: video_composition = "completed"
   ↓
6. 用户查询任务状态
   ↓
   [前端] GET /api/tasks/{task_id} → [API Gateway] → [Redis]
   ↓
7. 用户下载视频
   ↓
   [前端] 访问 video_url 下载视频文件
```

### 任务状态转换图

```
pending (初始状态)
  ↓
queued (已入队)
  ↓
processing (处理中)
  ├─► text_analysis: processing → completed
  ├─► image_generation: processing → completed
  ├─► voice_generation: processing → completed
  └─► video_composition: processing → completed
  ↓
completed (完成) / failed (失败)
```

---

## 存储架构

### Redis 存储

**Key 命名规范**:

```
task:{task_id}          # 任务数据（JSON 序列化）
tasks                   # 所有任务 ID 集合（Set）
```

**数据结构**:

```json
// task:{task_id}
{
  "task_id": "task_12345",
  "status": "completed",
  "novel_text": "...",
  "created_at": "2025-10-26T10:00:00Z",
  "updated_at": "2025-10-26T10:05:30Z",
  "progress": {
    "text_analysis": "completed",
    "image_generation": "completed",
    "voice_generation": "completed",
    "video_composition": "completed",
    "total_scenes": 5,
    "processed_scenes": 5
  },
  "video_url": "/tmp/tfboys/videos/task_12345/final.mp4"
}
```

**TTL 策略**:
- 任务数据: 7 天
- 队列消息: 1 天

### 本地文件存储

**目录结构**:

```
/tmp/tfboys/
├── scenes/              # 场景图像
│   ├── scene_001.png
│   ├── scene_002.png
│   └── ...
├── audio/               # 配音文件
│   ├── {task_id}/
│   │   ├── scene_001.mp3
│   │   ├── scene_002.mp3
│   │   └── ...
│   └── ...
└── videos/              # 生成的视频
    ├── {task_id}/
    │   ├── scene_001.mp4
    │   ├── scene_002.mp4
    │   └── final.mp4    # 最终视频
    └── ...
```

**文件命名规范**:
- 场景图像: `scene_{scene_id}.png`
- 配音文件: `{task_id}/scene_{scene_id}.mp3`
- 单场景视频: `{task_id}/scene_{scene_index:03d}.mp4`
- 最终视频: `{task_id}/final.mp4`

---

## 技术难点与解决方案

### 难点 1: 角色视觉一致性

**挑战**: 确保同一角色在不同场景中保持一致的外观

**解决方案**:
- 使用详细的提示词描述角色特征（年龄、发型、服装、配饰等）
- 在所有场景的 Prompt 中重复角色描述
- 维护角色描述库，确保描述一致性

**实现细节**:
```python
# 角色描述示例
character_desc = "15岁的少年，黑色短发，蓝色眼睛，穿着蓝白色校服"

# 场景 Prompt 模板
scene_prompt = f"""
场景描述: {scene.description}
角色: {character_desc}
风格: 动漫风格
画质: 高质量, 4K
"""
```

### 难点 2: 异步任务管理

**挑战**: 
- 多个异步任务的协调和状态管理
- 任务失败的重试和错误处理
- Worker 进程的健康管理

**解决方案**:
- 使用 Celery + Redis 实现分布式任务队列
- 实现细粒度的任务状态跟踪
- 配置任务重试策略和超时控制
- 实现任务失败时的回滚机制

**实现细节**:
```python
# Celery 配置
celery_app.conf.update(
    task_serializer='json',
    accept_content=['json'],
    result_serializer='json',
    timezone='Asia/Shanghai',
    enable_utc=True,
    task_track_started=True,
    task_time_limit=3600,        # 1小时超时
    task_soft_time_limit=3300,   # 55分钟软超时
    broker_connection_retry_on_startup=True,
)

# 重试装饰器
@retry_on_failure(max_retries=3, delay=2.0, backoff=2.0)
async def call_ai_service():
    # AI 服务调用
    pass
```

### 难点 3: 视频合成性能

**挑战**:
- FFmpeg 视频合成耗时较长
- 多场景视频占用大量磁盘空间
- 并发视频合成的资源竞争

**解决方案**:
- 异步处理，避免阻塞主线程
- 使用 FFmpeg 硬件加速（如果可用）
- 优化 FFmpeg 参数，平衡质量和速度
- 实现临时文件自动清理机制

**实现细节**:
```python
# FFmpeg 优化参数
ffmpeg_command = [
    'ffmpeg',
    '-hwaccel', 'auto',           # 自动选择硬件加速
    '-i', input_file,
    '-c:v', 'libx264',            # H.264 编码
    '-preset', 'medium',          # 编码速度（fast/medium/slow）
    '-crf', '23',                 # 质量参数（18-28，越小质量越高）
    '-c:a', 'aac',                # AAC 音频编码
    '-b:a', '128k',               # 音频比特率
    output_file
]
```

### 难点 4: AI 服务限流和稳定性

**挑战**:
- 七牛 AI Token API 可能有调用频率限制
- 网络请求可能超时或失败
- 大批量任务时需要控制并发

**解决方案**:
- 实现指数退避重试机制
- 使用信号量控制并发请求数
- 实现请求队列和限流策略
- 记录详细的错误日志用于排查

**实现细节**:
```python
# 重试装饰器
def retry_on_failure(max_retries=3, delay=1.0, backoff=2.0):
    def decorator(func):
        @wraps(func)
async def wrapper(*args, **kwargs):
            for attempt in range(max_retries):
                try:
                    return await func(*args, **kwargs)
                except Exception as e:
                    if attempt == max_retries - 1:
                        raise
                    await asyncio.sleep(delay * (backoff ** attempt))
        return wrapper
    return decorator

# 并发控制
semaphore = asyncio.Semaphore(5)  # 最多 5 个并发请求

async def call_ai_api():
    async with semaphore:
        # API 调用
        pass
```

---

## 扩展性设计

### 水平扩展

**服务层扩展**:
- API Gateway、AI Service、Video Service 均为无状态服务，可多实例部署
- 使用 Nginx 或云负载均衡进行流量分发

**Worker 扩展**:
```bash
# Docker Compose 扩展 Worker
docker compose up -d --scale ai-worker=4
docker compose up -d --scale video-worker=2
```

### 功能扩展

**AI 能力扩展**:
- 支持多种 AI 模型切换（GPT-4、Claude、Gemini 等）
- 支持自定义角色设定图上传
- 支持多种配音风格和语言

**视频风格扩展**:
- 支持多种视频风格（写实、动漫、水墨、3D 等）
- 支持自定义视频模板
- 支持视频特效和转场效果

**存储扩展**:
- 从本地文件系统迁移到对象存储（阿里云 OSS、七牛云等）
- 实现 CDN 加速视频访问

### 监控与运维

**服务监控**:
- 集成 Prometheus + Grafana 监控服务指标
- 使用 Celery Flower 监控任务队列
- 实现健康检查和告警机制

**日志管理**:
- 统一日志格式和级别
- 集中日志收集（ELK / Loki）
- 关键操作审计日志

---

## 部署架构

### 生产环境架构

```
┌────────────────────────────────────────┐
│         Nginx (负载均衡)               │
└───────┬────────────────────────────────┘
        │
        ├─► [前端静态资源] (CDN)
        │
        ├─► [API Gateway] (容器 x 2)
        │
        ├─► [AI Service] (容器 x 4)
        │
        └─► [Video Service] (容器 x 2)

共享层:
  - Redis Cluster (高可用)
  - 对象存储 OSS (文件存储)
  - RDS / PostgreSQL (元数据存储，可选)
```

### Docker 部署架构

参考 [安装与启动指南.md](./安装与启动指南.md) 中的 Docker 部署章节。

---

## 下一步

- 查看 [安装与启动指南.md](./安装与启动指南.md) 了解如何部署系统
- 查看 [团队分工文档.md](./团队分工文档.md) 了解模块职责和开发分工
- 查看 [CLAUDE.md](./CLAUDE.md) 了解开发规范和代码约定

---

**最后更新**: 2025-10-26  
**维护者**: TFBoys Team
